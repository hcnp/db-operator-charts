reconcileInterval: "10"
crds:
  keep: false
tests:
  enabled:
    - postgresql
  postgresql:
    admin:
      password: 123123!!
      user: postgres
    service:
      name: postgres-instance-postgresql
      namespace: databases
